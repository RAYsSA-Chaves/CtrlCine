# Configurações gerais do projeto

class Settings:
    API_STR: str = '/api'  # prefixo padrão para todas as rotas

    # infos do banco
    DB_HOST = "localhost"
    DB_USER = "root"
    DB_PASSWORD = "senai"
    DB_NAME = "CtrlCine"